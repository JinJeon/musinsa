import styled from 'styled-components';

export const StyledHeaderOptions = styled.div`
	display: flex;
	flex-direction: column;
	gap: 0.4rem;
`;

export const StyledFilterBtns = styled.div`
	display: flex;
	flex-wrap: wrap;
	gap: 0.2rem;
`;

export const StyledSelectedFilter = styled.div`
	display: flex;
	flex-wrap: wrap;
	gap: 0.2rem;
`;
